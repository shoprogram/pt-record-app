# Claude用プロジェクトルール

## ブランチ運用
- main(master)ブランチには直接コミットしないこと
- 必ず新しいブランチを作成して作業すること
- 作業後は必ずPull Requestを作成すること

## テスト・チェック
作業後は必ず以下を実行すること

- `npm run lint`
- `npm run test:unit`

必要に応じて以下も実行すること

- `npm run test:e2e`
- 実行できない場合は、理由と必要な権限を明記すること

## GitHub Actions / workflow
- `.github/workflows/` 配下のファイルは絶対に変更しないこと
- workflowの変更が必要な場合は、変更案のみ提示すること（実際の変更は行わない）

## コード変更方針
- 変更は必要最小限にすること
- 既存のディレクトリ構成・命名規則に従うこと
- 不要なリファクタリングは行わないこと

## PR（Pull Request）
- 変更内容を簡潔にまとめること
- 実行したコマンドと結果を記載すること
- テスト結果を記載すること
- エラーが出た場合は原因と対応方針を書くこと
- PRを作成できない場合は、できなかった理由を明記すること
